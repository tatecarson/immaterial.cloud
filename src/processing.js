import p5 from 'p5'
import 'p5/lib/addons/p5.sound'

// create here so you can change them elsewhere and they aren't locked inside init()

// this seems to be working
export const delay = new p5.Delay()
export const reverb = new p5.Reverb()